# Expense Manager — Frontend

React (Vite) + Tailwind CSS client for the Expense Manager API.

## Setup
1. `npm install`
2. Copy `.env.example` to `.env` and point `VITE_API_BASE_URL` at your running Laravel API.
3. `npm run dev`

## Testing
`npm test`
